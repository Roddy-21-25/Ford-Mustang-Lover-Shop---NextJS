import { Cinzel_Decorative } from "next/font/google";

export const Cinzel = Cinzel_Decorative({ subsets: ["latin"], weight: ["400"] });
export const CinzelBolder = Cinzel_Decorative({ subsets: ["latin"], weight: ["700"] });
